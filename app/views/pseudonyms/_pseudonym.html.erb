<% content_for :timestamp do %>
  <%= link_to (pseudonym.updated_at || pseudonym.created_at).strftime("%Y-%m-%d %k:%M:%S"), pseudonym %>
<% end %>

<%= render partial: 'directory/card', locals: {
  model: pseudonym, variant: (defined?(variant) && variant) || 'full' 
} %>