<h1>New announcement</h1>

<div class="card primary">
  <%= render "form", announcement: @announcement %>
</div>

<div class="card primary">
  <%= link_to "Back to announcements", announcements_path, class: "button" %>
</div>
