<h1>Filters</h1>

<%= render partial: 'filters/filter_sidebar', layout: 'directory/left_sidebar',
    locals: { with_search: true }
%>

<%= render partial: 'filters/filters', locals: {filters: @filters} %>
