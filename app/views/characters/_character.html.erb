<% if defined?(locals) && locals[:reporting] %>
  <% character = locals[:reportable] %>
<% end %>

<%= render partial: 'directory/card', locals: { 
  model: character, variant: (defined?(variant) && variant) || 'full' 
} %>