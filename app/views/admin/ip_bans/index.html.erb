<div class="card primary">
  <h1>IP Bans</h1>
  <%== pagy_nav @pagy %>
  <p>
    <%= link_to "New IP Ban", new_admin_ip_ban_path, class: "button" %>
  </p>
</div>

<div id="ip_bans">
  <% @ip_bans.each do |ip_ban| %>
    <div class="card secondary">
      <%= render ip_ban %>
      <div class="input-group">
        <%= link_to "Show", url_for([:admin, ip_ban]), class: "button" %>
      </div>
    </div>
  <% end %>
</div>

<div class="card primary">
  <%== pagy_nav @pagy %>
</div>
