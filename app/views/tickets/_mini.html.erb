<% content_for "card_content_#{ticket.id}" do %>
  <% if ticket.item_type.present? %>
    <%= link_to "#{ticket.item_type} #{ticket.item_id}", ticket %>
  <% else %>
    No ticket item attached.
  <% end %>
<% end %>

<%= render partial: 'directory/card', locals: { model: ticket } %>